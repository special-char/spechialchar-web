import { ReactNode } from "react";
export type CardType={
  
    data:{
        id:number;
    url:string;
    }
}

export type CollaboratorCardType={
    data:{
        id:number;
        url:string;
        title:string;
        description:string;
    }
}
